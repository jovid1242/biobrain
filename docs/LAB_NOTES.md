# Научный журнал

Формат записи: DATE · HYPOTHESIS · SETUP · RESULT · INTERPRETATION · PROBLEMS · NEXT.
RESULT — только то, что измерено или прочитано в источнике. INTERPRETATION — наше понимание, может оказаться неверным.

---

## LN-001 · 2026-09-16 · Исследование источников

**HYPOTHESIS.** FlyWire FAFB 783 остаётся лучшей стартовой точкой для графового исследования; существуют
первичные числа, с которыми можно сверить собственную обработку.

**SETUP.** Три независимых прохода по первоисточникам (flywire.ai, Codex, Zenodo API, GitHub API, Nature/Cell/
bioRxiv/arXiv, neuPrint/Janelia): (A) продукты данных FlyWire и их статистика; (B) все доступные коннектомы
дрозофилы; (C) предшествующие работы по коннектому как архитектуре. Требование — дословная цитата + URL на каждое
число. Выборочная ручная перепроверка: Zenodo API (файлы, размеры, md5, лицензия, версии записи), GitHub API
(теги, blob SHA), цитаты Dorkenwald/Schlegel в сохранённых текстах, arXiv API для пяти работ 2025–2026.

**RESULT.**
- 783 — последний публичный релиз FAFB (flywire.ai, Codex). Запись Zenodo 10676866 существует в единственной
  версии (783.0, 2024-06-02), 5 файлов, 10.6 GB, лицензия CC BY 4.0; flywire.ai указывает CC BY-NC 4.0.
- После 783 изменились аннотации (v3.1.0, 2026-07-21) и появился альтернативный набор синапсов (Princeton,
  preprint 2025, в Codex по умолчанию с июля 2025).
- Опубликованные суммы расходятся на уровне десятков тысяч синапсов и ~1,000 связей между источниками (таблица в `RESEARCH.md` §9).
- Счёт суперклассов в Schlegel 2024 суммируется в 127,864, а не в 139,255.
- В литературе преимущество топологии коннектома против degree-preserving null-моделей обычно исчезало или
  было небольшим; самый сильный позитив — FlyGM (2026, RL-контроллер тела), без описанного контроля общей инициализации.

**INTERPRETATION.** Выбор FAFB 783 + синапсы Buhmann обоснован воспроизводимостью и возможностью сверки.
Сумма 127,864 указывает на то, что счёт в статье сделан по более раннему снимку (вероятно, 630) — это надо
учитывать при валидации. Априорная вероятность «чистого» выигрыша H1 невысока — это аргумент за строгий протокол, а не против проекта.

> **ПОПРАВКА (2026-09-17, см. LN-003).** Объяснение «более ранний снимок» оказалось **неверным**. На аннотациях
> v2.1.0 все числа статьи совпали точно; 11,391 «недостающих» нейронов — зрительные сенсорные, которых фраза
> статьи просто не перечисляет. Запись оставлена как есть, чтобы ошибка была видна.

**PROBLEMS.** Часть полных текстов закрыта (Cell, ScienceDirect, bioRxiv rate limit); использованы зеркала
(Europe PMC, PMC, arXiv) — помечено. Codex-портал и CAVE требуют логина и не проверялись.

**NEXT.** Скачать минимальный набор, проверить эти числа на данных.

---

## LN-002 · 2026-09-16 · Загрузка данных: сбой Zenodo

**HYPOTHESIS.** Шесть файлов (~1.1 GiB) скачиваются скриптом и совпадают с официальными checksum.

**SETUP.** `biobrain download` (каталог `catalog/flywire_fafb_v783.toml`).

**RESULT.**
- GitHub: обе версии аннотаций скачаны, git blob SHA-1 совпали с GitHub API (v3.1.0: 31,718,505 байт; v2.1.0: 27,015,208).
- Zenodo: с ~21:40 (UTC+5) все запросы к файлам — HTTP 504 Gateway Time-out через ~30 с; позже 504 отдавали
  и API записей, и главная страница zenodo.org перестала отвечать. Сбой на стороне Zenodo, не зависит от записи.
- v3.1.0 содержит **139,248** строк при 139,255 нейронах; v2.1.0 — 139,255.

**INTERPRETATION.** Первая версия загрузчика останавливалась на первом сбойном файле и буферизовала лог —
исправлено: повторы с докачкой, изоляция ошибок по файлам, построчный лог. Публичный bucket Codex мог бы
заменить Zenodo, но это другой формат (без 6 вероятностей нейромедиатора и без счётчиков по neuropil) и
недокументированный путь — без решения владельца не используется (D-011).

**PROBLEMS.** Пока Zenodo недоступен, preprocess/validate/analyze на реальных данных не выполнены.
Весь код проверен на синтетических релизах того же формата (48 тестов).

**NEXT.** Дождаться Zenodo (фоновые повторы), затем preprocess → validate → analyze → bench-memory; выяснить, каких 7 нейронов нет в v3.1.0.

---

## LN-003 · 2026-09-17 · Аннотации: две версии против статьи

**HYPOTHESIS.** (а) Счёт суперклассов в Schlegel 2024 описывает не релиз 783 (моё вчерашнее объяснение суммы 127,864).
(б) v3.1.0 отличается от v2.1.0 только на 7 нейронов.

**SETUP.** Обе TSV (проверены по git blob SHA-1) против `proofread_root_ids_783.npy` (проверен по md5). Отдельный скрипт; формально повторяется в `biobrain validate` (H1–H4, G1–G5).

**RESULT.**
- v2.1.0: 139,255 строк, множество root id **совпадает** со списком 783. Суперклассы: optic 77,536; central 32,388; sensory 16,903; visual_projection 8,053; ascending 2,362; descending 1,303; visual_centrifugal 524; motor 106; endocrine 80.
- sensory = 11,391 (cell_class `visual`) + **5,512** незрительных — ровно число статьи; остальные 8 классов совпадают со статьёй точно.
- Типы: `cell_type` — 5,634 значения (79.8 % нейронов); «`cell_type`, иначе `hemibrain_type`» — **8,453** значения (число статьи точно), покрытие 94.65 % (в статье 96.4 %).
- v3.1.0: 139,248 строк; **7** root id нет в списке 783; у **14** нейронов из списка 783 нет строки (по v2.1.0: 1 центральный CX, 3 механосенсорных JO-B, 10 оптических). Суперкласс изменился у 639 общих нейронов (612 из них: ascending → новый `sensory_ascending`), cell_type — у 47,612.
- Чтение v2.1.0 падало: `nucleus_id` и `soma_x` записаны как `2453924.0`. Исправлено: целочисленные id читаются из текста (точно и для значений > 2⁵³).

**INTERPRETATION.** Гипотеза (а) опровергнута: статья описывает именно 783, просто формулировка неполная.
Гипотеза (б) неверна: разница больше (7 заменённых/новых id + 14 без строки) и типизация сильно переработана.
v3.1.0 остаётся рабочей версией (больше типов, 98.9 % нейронов с `cell_type`), но 14 нейронов получают пустые
аннотации, а сравнения со статьями делаются только на v2.1.0.

**PROBLEMS.** Что стоит за 7 новыми id (переразбиение сегментов после 783?), без CAVE не проверить. Знаменатель 96.4 % не найден.

**NEXT.** Полная валидация после загрузки connection table.

---

## LN-004 · 2026-09-17 · Валидация графа на реальных данных

**HYPOTHESIS.** Наш конвейер воспроизводит все опубликованные итоги FlyWire 783 без подгонки; инварианты хранилища выполняются.

**SETUP.** `biobrain preprocess` → `biobrain validate` (commit `d47ea8e`). 54 проверки: инварианты, числа из
статей и Codex с указанием источника, независимый пересчёт итогов через Arrow `group_by`.

**RESULT.**
- Первый прогон: 29 PASS, **3 FAIL**, 8 WARN. Разбор показал **две ошибки обработки** и одну ложную проверку:
  1. релиз обозначает «нет neuropil» строками `UNASGD` (таблица связей) и `"None"` (счётчики); код считал их регионами → 79 «neuropil» вместо 78 и несовпадения итогов с Codex;
  2. у 1,732 строк все шесть вероятностей нейромедиатора — NaN; код занижал вероятности у 481 ребра, деля на все синапсы, включая непредсказанные;
  3. проверка «сумма вероятностей = 1» не учитывала рёбра без предсказаний.
- После исправлений (с тестами на синтетических данных с теми же аномалиями): **38 PASS, 0 FAIL, 3 WARN, 13 INFO**.
- Точно воспроизведены: 139,255 нейронов; 16,847,997 строк; 15,091,983 пары; 54,492,922 синапса; 2,700,513 пар ≥ 5 между 134,181 нейронами; 130,054,535 / 130,038,118 / 121,904,312 / 58,096,025 синапсов (Zenodo/Codex); 78 neuropil; суперклассы и 8,453 типа Schlegel 2024.
- Self-connections: 0. Изолированных нейронов: 616. Медианная доля пресинапсов нейрона с proofread-партнёром: 52 %.

**INTERPRETATION.** Данные в store совпадают с первоисточниками до синапса. Главный урок: «скрипт завершился
без ошибки» ничего не значил. Обе ошибки давали правдоподобные числа и были пойманы только сверкой с
независимыми итогами. Число 2,701,601 у Lin 2024 для 783 наши данные не подтверждают (подтверждают 2,700,513 Dorkenwald 2024).
Граф заметно неполон со стороны выходов нейронов: это ограничение для любой модели на его основе.

**PROBLEMS.** Остались 3 WARN, не зависящие от нас (annotation drift v3.1.0, знаменатель 96.4 %).

**NEXT.** Замеры памяти; анализ графа.

---

## LN-005 · 2026-09-17 · Представления графа: память против скорости

**HYPOTHESIS.** Компактное представление (CSR) достаточно; сжатие (varint, квантование) даст выигрыш по памяти без существенной потери скорости.

**SETUP.** `biobrain bench-memory` (commit `d47ea8e`): 13 представлений, каждое в отдельном процессе. Первый прогон
дал завышенный RSS: в него попадали страницы memmap-источника и временные массивы тестов доступа. Методика
исправлена (RSS сразу после построения, источники без memmap), прогон повторён.

**RESULT.** Байт на ребро: Python-объекты 128–256 (экстраполяция), Arrow «как скачано» 90.7, COO int64 24,
COO compact 10, **CSR 6.07 (87 MiB на весь мозг)**, CSR+CSC 14.15, delta+varint 3.02, zstd 3.29, region-local ids 5.20.
Шаг спайков (1 % нейронов, 143,676 событий): CSR 0.51 мс, COO 10.7–15.7 мс, varint 3.27 мс. Полный проход: CSR 25 мс, varint 369 мс.
Веса: uint16 без потерь (максимум 2,405); uint8-лог ошибается ≤ 7.1 % на ребро.

**INTERPRETATION.** Гипотеза частично опровергнута: сжатие вдвое (varint) стоит 6.4× по шагу спайков и 15× по
полному проходу, и при графе в 87 MiB это невыгодно. Region-local ids дают только −14 %, потому что лишь 47.6 %
рёбер лежат внутри home neuropil. Узкое место для 8 GB будет не граф, а рабочая память и состояния модели.

**PROBLEMS.** Скорости измерены в NumPy из Python; это сравнение представлений, а не производительность будущего симулятора.

**NEXT.** Анализ графа (идёт).

---

## LN-006 · 2026-09-17 · Структура графа против null-моделей

**HYPOTHESIS.** (а) Анализатор на релизе 783 воспроизводит сетевую статистику Lin et al. 2024 (релиз 630).
(б) Граф мухи отличается от degree-preserving random не только распределением степеней.

**SETUP.** `biobrain analyze` (commit `063d2be`, seed 20260916, 2,150 с, peak RSS 3.05 GiB): графы ≥ 1 и ≥ 5
синапсов; ER и 5 degree-preserving null-моделей; triad census; Leiden; rich club; sampled betweenness; cost gate
на каждом дорогом шаге.

**RESULT.** (полностью — `docs/RESULTS.md`)
- ≥ 5: reciprocity 0.1398 (null ×45.8), clustering 0.0448 (null ×7.95), путь 4.45, S^Δ 156, медиана степеней intrinsic 11/13, Pearson 0.757.
- Мотивы: открытые цепочки ×0.51–0.66 от null; 030T ×4.6; 030C ×1.75; взаимные тройки ×10–520.
- Leiden Q 0.712 (без весов 0.698 против 0.126 у null).
- Rich club: ≤ +3.2 % при степени 24–91, ниже null при степени ≥ 102.
- Betweenness: top-100 двух выборок пересекаются на 50 %.
- Оценщик triad census ошибся в 3.2–4.4 раза в меньшую сторону.

**INTERPRETATION.** (а) Подтверждено: совпадение с Lin по порядку и часто до второго знака. (б) Подтверждено:
reciprocity, clustering, модульность и замкнутые мотивы далеко за пределами degree-preserving null. Для M4 это
означает, что контроль «перемешанная муха» должен быть лестницей null-моделей, а не одной моделью. Rich club
как «существенное свойство» данными **не поддержан**. Betweenness на отдельных нейронах при 256 источниках —
не результат.

**PROBLEMS.** Калибровка стоимости на ER не переносится на тяжёлые хвосты. 5 null-выборок мало для редких мотивов.
Triad census на графе ≥ 1 не посчитан.

**NEXT.** Отчёт Milestone 1; ожидание решения владельца по Milestone 2.

---

## LN-007 · 2026-09-17 · M2: корректность движка и эквивалентность режимов

**HYPOTHESIS.** Один LIF-движок в двух режимах (time-step, event-driven) даёт одинаковую динамику на реальных подграфах FlyWire.

**SETUP.** 58 модульных и интеграционных тестов: LIF, порог, сброс, рефрактерность, задержка, E/I, крайние случаи.
Эквивалентность на 12 случайных сетях с разными τm, рефрактерностью, задержкой, v_reset. На реальных подграфах
100 / 1k / 10k / 50k / LO_R: 4 уровня входа × float64/float32 × 2 варианта агрегации = 80 сравнений.

**RESULT.** 78 из 80 сравнений в пределах заранее объявленного допуска. Растры идентичны во всех 80 сравнениях,
число спайков совпадает во всех 180 парах основной матрицы (уточнено в LN-014). float64: максимальная разница потенциалов 8.5·10⁻¹⁴. Два превышения:
LO_R, float32, 1.5·10⁻⁴ против допуска 10⁻⁴ при v ≈ −396 (относительная разница 3.9·10⁻⁷).

**INTERPRETATION.** Режимы эквивалентны по спайкам. Абсолютный допуск float32 плохо подходит для больших |v|.
Большие |v| сами по себе показывают, что у модели нет нижней границы потенциала (веса до 342θ, v до −771θ).

**PROBLEMS.** Потенциал без нижней границы — ограничение модели, а не движка. **NEXT.** Калибровка и бенчмарки.

---

## LN-008 · 2026-09-17 · M2: режимы активности и калибровка gain

**HYPOTHESIS.** Для реальных подграфов существует диапазон gain, где сеть не мертва и не насыщена.

**SETUP.** 19 значений gain (0.003–3, логарифмически) × 3 seed; вход Пуассон 1 % на шаг 300 мс, затем 300 мс
тишины. Правила DEAD / STABLE / SATURATED записаны до прогонов.

**RESULT.** Устойчивый диапазон есть у 6 из 7 подграфов:

| подграф | диапазон | baseline |
|---|---|---|
| expand_100 | 0.03–0.14 | 0.065 |
| expand_1k | 0.014–0.065 | 0.030 |
| expand_10k | 0.009–0.095 | 0.030 |
| expand_50k | 0.020–0.30 | 0.078 |
| SPS_R | 0.03–0.65 | 0.139 |
| LO_R | 0.44–0.65 | 0.533 |

- **neuropil_PB мёртв при любом gain**, и рост gain уменьшает активность (усиление 0.98 → 0.55). При глутамате = +1
  устойчивый диапазон появляется: 0.044–0.065.
- Во многих устойчивых точках больших подграфов активность сохраняется без входа.

**INTERPRETATION.** Переход DEAD → STABLE → SATURATED по gain есть почти везде, но положение и ширина окна
зависят от подграфа. У LO_R окно узкое и резкое. Мёртвость PB объясняется допущением «глутамат тормозной»:
53 из 102 нейронов PB глутаматергические.

**PROBLEMS.** Разные gain для разных подграфов. **NEXT.** Бенчмарки на baseline gain.

---

## LN-009 · 2026-09-17 · M2: event-driven против time-step — где перелом

**HYPOTHESIS.** При разреженной активности event-driven быстрее time-step; перелом существует и измерим.

**SETUP.** 100/1k/10k × {time-step, event-driven sparse, event-driven auto} × 10 уровней входа (0.001–50 %) × 3 seed,
1,000 шагов, каждый прогон в отдельном процессе (270 прогонов). Дополнительно нейропили, паттерны входа, профиль.

**RESULT.**
- Максимальное ускорение event-driven: 6.6× (100 нейронов, вход 0.001 %), 3.0× (1k), 1.22× (10k, auto). Разреженный
  вариант на 10k не быстрее ни при какой активности.
- Перелом (медиана ускорения = 1):

  | подграф | вход | событий на нейрон за шаг |
  |---|---|---|
  | 100 | ≈0.04–0.06 % | ≈0.08–0.12 |
  | 1k | ≈0.005 % | ≈0.02 |
  | 10k | ≈0.0015 % (auto) | ≈0.015 |
  | SPS_R | ≈0.003–0.005 % | |
  | LO_R | нет: event-driven медленнее везде (сильное усиление слабого входа; ~~самоподдерживающаяся активность~~ — исправлено в LN-014) | |

- При 1 % входа event-driven медленнее в 2.5–13 раз (исправлено в LN-014, было «4–13») и пропускает лишь 21–65 % обновлений нейронов.
- Профиль: в time-step 54–94 % времени при ≥ 0.1 % входа уходит на доставку синаптических событий (исправлено в LN-014, было «70–95 %»). В event-driven
  до 83 % занимает сортировка событий, остальное — ленивые gather/scatter.

**INTERPRETATION.** В этой реализации event-driven выигрывает, только если пропускает ≳ 93–99 % обновлений (исправлено в LN-014, было «≳ 99 %»). С ростом
сети перелом сдвигается к **меньшей** активности, против обычного ожидания. Причины: шаг NumPy по всем нейронам
почти бесплатен, число событий на спайк растёт с плотностью подграфа, а синаптические операции одинаковы в обоих
режимах и доминируют. Это вывод о «NumPy + плотные подграфы», а не о event-driven вообще.

**PROBLEMS.** Результаты зависят от реализации. Машина без вентилятора работала от батареи. **NEXT.** 50k,
оценка энергии, план компилируемого backend.

---

## LN-010 · 2026-09-17 · M2: чувствительность и null-топологии

**HYPOTHESIS.** Динамика слабо зависит от допущений, которые FlyWire не определяет.

**SETUP.** expand_1k и SPS_R на baseline gain, 16 вариантов, 3 seed. Null-топологии на expand_1k: degree-preserving и
reciprocity-preserving, 3 seed.

**RESULT.**
- expand_1k:
  - глутамат = +1 → **SATURATED**: 111 Гц против 6.3 Гц, усиление 11.4;
  - sqrt и log1p → **DEAD**;
  - clipped(20) → на грани;
  - ≥ 5 синапсов → в основном DEAD;
  - τm 10 / 40 мс → 7.7 / 5.4 Гц;
  - gain ×2 → 17.5 Гц;
  - знак модуляторов, источник знака (нейрон или ребро), рефрактерность, задержка → изменения ≤ 20 %.
- SPS_R: глутамат = +1 → SATURATED (100 Гц), остальные варианты STABLE.
- Null-модели (expand_1k):
  - реальный граф: взаимность 0.382, 6.3 Гц;
  - degree-preserving: 0.116, 16–17 Гц;
  - reciprocity-preserving: 0.428 (приближённо; 2.9 % рёбер отброшено), 12 Гц.

  Эквивалентность режимов соблюдена во всех трёх.

**INTERPRETATION.** Гипотеза опровергнута. Динамика сильно зависит от знака глутамата, преобразования весов и порога
по числу синапсов, умеренно от τm и gain, слабо от остального. Любые будущие сравнения топологий (M4) должны
фиксировать эти допущения одинаково для всех условий и повторяться при альтернативах. Различия реального графа и
null-моделей по частоте видны, но это не вывод о топологии: gain калиброван только для реального графа.

**PROBLEMS.** Reciprocity-preserving null приближённый. **NEXT.** Отчёт M2.

---

## LN-011 · 2026-09-17 · M2: 50k и режим в рабочей точке

**HYPOTHESIS.** 50k нейронов выполнимы на этой машине, а тенденции 100 → 10k сохраняются.

**SETUP.** expand_50k (7,611,276 рёбер в симуляции): 10 уровней входа × {time-step, event-driven sparse, auto} × 2 seed
× 300 шагов, изолированные процессы. Отдельно — режим на baseline gain всех 6 подграфов тем же пробным входом, что
при калибровке (3 seed), потому что baseline 50k и LO_R не попадает в точку сетки.

**RESULT.**
- Peak RSS ≤ 507 MiB (417 MiB сверх пустого процесса). time-step: 0.21–6.7 с на секунду модели.
- event-driven медленнее при всех уровнях входа: sparse 0.07–0.10×, auto 0.17–0.56×.
- При входе 0.001 % сеть даёт 3.5 Гц на нейрон, пропускается 67 % обновлений.
- На baseline у всех 6 подграфов 3 из 3 seed STABLE. Активность держится после входа у 10k в 2 из 3 seed, у 50k в
  3 из 3, у остальных — ни в одном.

**INTERPRETATION.** Сдвиг перелома к меньшей активности с ростом N доходит до его исчезновения. На 50k активность
самоподдерживается, поэтому слабый вход не делает её разреженной. Память растёт линейно с числом рёбер и
ограничением не является.

**PROBLEMS.** 2 seed и 300 шагов. **NEXT.** Длинная эквивалентность, оценка энергии.

---

## LN-012 · 2026-09-17 · M2: длинные прогоны float32

**HYPOTHESIS.** Режимы остаются эквивалентными и на длинных прогонах.

**SETUP.** В оценке энергии прогон на 40,000 шагов (float32) дал разное число спайков в двух режимах. Повтор:
expand_10k, вход 0.1 %, seed 1, 40,000 шагов, float32 и float64, time-step и event-driven auto, запись растров.
Сравнивался первый шаг, на котором растры различаются.

**RESULT.**
- float64: растры идентичны все 40,000 шагов (2,827,452 спайка).
- float32: time-step отходит от float64 на шаге **3,349**; к концу 2,837,473 спайка (+0.35 %).
- event-driven float32 идентичен float64 все 40,000 шагов.

**INTERPRETATION.** Гипотеза опровергнута для float32. Алгоритмы эквивалентны; расходится округление, и в
рекуррентной сети первое перевернувшееся пересечение порога размножается. Предполагаемый механизм: time-step
умножает на округлённое α на каждом шаге, event-driven — на α^k, округлённое один раз (таблица float32, посчитанная
в float64). *(Формулировка уточнена в LN-019.)* Короткие
прогоны (≤ 1,000 шагов) по-прежнему совпадают во всех проверенных случаях.

**PROBLEMS.** Один подграф, один вход, один seed. **NEXT.** Эталон для длинных сравнений — float64.

---

## LN-013 · 2026-09-17 · M2: оценка энергии от ОС

**HYPOTHESIS.** Оценка энергии, доступная без root, несёт информацию сверх времени CPU.

**SETUP.** `proc_pid_rusage` (RUSAGE_INFO_V6, `ri_energy_nj`, модель ядра macOS). expand_10k, вход 0.001 %, 0.1 % и
10 %; 5 повторов time-step и event-driven auto вперемешку; окна ≥ 1.5 с. Питание от батареи, Low Power Mode выключен.

**RESULT.** 6.6–7.8 Дж (оценка) на секунду CPU. Отношения ED/TS по энергии — 0.67 / 5.72 / 2.92, по времени —
0.76 / 5.94 / 3.02. Разброс отношения по энергии между повторами ≤ 1.5 %, по времени — 1–9 %.

**INTERPRETATION.** Гипотеза опровергнута: оценка почти пропорциональна времени CPU. Для выводов об энергии нужен
`powermetrics` (root) или внешний прибор. Это решение владельца.

**PROBLEMS.** Учитывается только CPU процесса: без памяти, GPU и остальной системы. **NEXT.** Отчёт M2.

---

## LN-014 · 2026-09-17 · M2: исправления предыдущих записей

Перед отчётом M2 все утверждения документов сверены с сырыми файлами. Неточности:

- LN-009: доля доставки событий в time-step — **54–94 %**, а не 70–95 % (на 1k при входе 0.1 % — 54 %).
- LN-009: перелом наступает при пропуске **≳ 93–99 %** обновлений, а не ≳ 99 % (на 100 нейронах — 94–96 %).
- LN-009: при входе 1 % event-driven медленнее в **2.5–13 раз**, а не в 4–13 (на 100 нейронах — в 2.5–3.1).
- LN-007: в парах основной матрицы сравнивалось **число спайков**, а не растры (растры — только в 80 сравнениях).
- LN-009: у LO_R на baseline **нет самоподдержания** (0 из 3 seed). Event-driven медленнее из-за сильного
  усиления слабого входа: 106 внешних событий дают 16,864 спайка, всплески до 10 % нейронов за шаг.
- `PERFORMANCE_PLAN.md`, `SIMULATOR.md`: граница микробенчмарка уточнена. Вместо «E > ~N/10»: при N ≥ 10k
  сортировка выигрывает до E = N/10, плотный вариант — от E = N/5.
- Оценка полного мозга: доля рёбер с нулевым знаком теперь считается по store (5.5 %), а не медианой подграфов (≈ 3 %).
  В графиках RAM, калибровки и профиля исправлены отображение и подписи; сырые данные не менялись.

---

## LN-015 · 2026-09-17 · M2.5: окружение, baseline, профиль до оптимизации

**HYPOTHESIS.** Проигрыш event-driven в M2 в основном вызван накладными расходами Python/NumPy.

**SETUP.**
- До кода: тесты, sha256 store и manifest проверены, сырые результаты M2 заморожены по sha256.
- Numba 0.67.0 / llvmlite 0.49.0 поставлены в основной `.venv`: у них есть wheels для Python 3.14, NumPy не
  менялся.
- Профиль NumPy «до»: таймеры фаз, cProfile, счёт байткодовых операций через `sys.monitoring`, tracemalloc;
  1k/10k/50k × 5 входов × 3 режима.

**RESULT.**
- **Профиль подтвердил M2:**
  - NumPy TS: gather + доставка — 54–96 % шага при входе ≥ 0.1 %;
  - NumPy ED: ленивая утечка — 26–67 %, группировка событий — 12–49 %;
  - 24–41 (TS) и 3–99 (ED) вызовов на шаг плюс 10–58 операторов и индексаций;
  - временная память до 44 MiB.
- **M2 воспроизводится:** 72 NumPy-прогона дали те же спайки, время — 0.996 от M2 (медиана).

**INTERPRETATION.** В NumPy ED основная цена — обслуживание: вызовы, временные массивы, gather/scatter. Проверять
гипотезу нужно компиляцией.

**NEXT.** Скомпилированный time-step.

---

## LN-016 · 2026-09-17 · M2.5: скомпилированные ядра и корректность

**HYPOTHESIS.** Скомпилированные ядра могут побитово воспроизвести NumPy, если повторить порядок операций.

**SETUP.**
- Numba-ядра TS и ED без выделений в цикле; три варианта накопления в ED.
- Концептуальные тесты M2 на каждом ядре; побитовые сравнения на реальных подграфах и полном мозге, в float32 и
  float64, до 40,000 шагов.
- Счётчики аллокаций NRT на 50 и 800 шагах.

**RESULT.**
- **Побитово равны NumPy:** 16/16 (TS), 45/45 (ED, три варианта), 64/64 (1k–139k × 4 входа × 2 типа), 40,000 шагов
  в обоих типах.
- **Аллокации:** 18 (TS) и 31 (ED) на прогон, при 50 и 800 шагах одинаково.
- **Первая версия ED** (с ветвлениями) тратила 12–16 нс на обновление нейрона, без ветвлений — 2–3 нс при той же
  арифметике.
- **Compiled TS против compiled ED в float32** расходятся на шаге 3,349 — том же, что NumPy в M2.

**INTERPRETATION.** Гипотеза подтверждена. Совпадение шага 3,349 у независимой реализации подтверждает, что
float32-расхождение режимов — порядок операций, а не ошибка. Скорость ED сильно зависит от деталей реализации,
поэтому сравнения проводились только на финальных ядрах без ветвлений.

**NEXT.** Изолированная матрица.

---

## LN-017 · 2026-09-17 · M2.5: event-driven после компиляции — исход C

**HYPOTHESIS.** После компиляции event-driven станет быстрее time-step при реалистичной активности (исход A).

**SETUP.** 1k/10k/50k × вход 0.001–50 % × {NumPy TS, NumPy ED auto, compiled TS, compiled ED} × 3 seed × 1,000 шагов.
216 изолированных процессов в перемешанном порядке, от сети. Классификация A/B/C объявлена в плане до прогонов.

**RESULT.**
- **Ускорение от компиляции:** TS в 1.3–11 раз, ED в 5–53 раза.
- **Compiled ED против compiled TS:**
  - 1k: 28× (0.001 %), 4.7× (0.01 %), 1.15× (0.1 %), 0.49–0.58× (≥ 1 %);
  - 10k: 21×, 3.0×, затем 0.49–0.63×;
  - 50k: 0.50–0.75× везде.
- **Исход C** в 5 из 6 клеток реалистичной активности.
- **Перелом:** 1k — 0.16 % входа (0.39 события на нейрон за шаг, 72 % пропущено); 10k — 0.048 % (0.43, 69 %).
  В NumPy было 0.0056 % и 0.0014 %.
- **Профиль после компиляции:** доставка событий — 55–95 % в TS при входе ≥ 1 %, 50–82 % в ED при ≥ 0.1 %.
  0.56 нс на нейрон в TS против ≈ 2 нс на затронутый нейрон в ED.
- **Предел любой ED-реализации** (без затрат на нейроны): 1.1–1.9× на 50k, до 2.9× на 10k при 0.1 %.

**INTERPRETATION.** Гипотеза A опровергнута. Компиляция сдвинула перелом примерно в 30 раз по входу, но
реалистичная активность осталась за ним. Узкое место — синаптические события, одинаковые в обоих режимах.

**NEXT.** Полный коннектом.

---

## LN-018 · 2026-09-17 · M2.5: полный коннектом (139,255 нейронов)

**HYPOTHESIS.** Полный мозг безопасно запускается на этой машине; по оценке M2 процессу хватит ~0.2 GiB.

**SETUP.**
- ESTIMATE до запуска по compiled-моделям, сборка сети в отдельном процессе.
- Калибровка по протоколу M2 (compiled TS; сверка с NumPy побитово при двух gain) и режим на baseline.
- Эквивалентность; smoke test: 5 входов × 4 backend × 3 seed; проба «500 мс входа, 500 мс без входа».

**RESULT.**
- **Калибровка:** DEAD ≤ 0.014, STABLE 0.03–0.44, SATURATED ≥ 0.65; baseline 0.115; на baseline STABLE и
  самоподдержание во всех 3 seed. Заняла 30 с.
- **Время:** compiled TS 0.23–1.31 с на секунду модели, compiled ED 0.41–2.45 с (0.53–0.61× от TS), NumPy TS
  0.76–4.4 с, NumPy ED 3.9–14 с.
- **Память:** peak RSS compiled 268–327 MiB, NumPy 229–404 MiB; сборка сети 935 MiB.
- **Активность:** 6–60 Гц; активных за 1 с 23–78 %; после выключения входа активность держится на 4–10.5 Гц и ни
  разу не падает до нуля (9/9 прогонов).
- **Качество ESTIMATE M2:** RAM занижена на 17 % (206 против 247 MiB); модель времени NumPy ошиблась в медиане на
  8 %. Предполагалось 102 события на спайк, фактически 142–180.
- **Экстраполяция compiled-моделей** с 1k–50k занизила время на полном мозге на 35–39 %: стоимость события
  растёт с размером (0.6 → 1.8 нс).

**INTERPRETATION.** Гипотеза подтверждена по безопасности; оценка RAM оказалась занижена. Полный мозг на
baseline самоподдерживает активность, поэтому слабый вход не делает её разреженной и event-driven не выигрывает.
Рост стоимости события похож на ограничение памятью — гипотеза без аппаратных счётчиков.

**PROBLEMS.** Самоподдержание может быть свойством модели, а не проводки. **NEXT.** Отчёт M2.5.

---

## LN-019 · 2026-09-17 · Уточнение к LN-012 и M2_RESULTS §2

В M2 механизм float32-расхождения был описан так: event-driven «берёт α^k из таблицы float64 и округляет только
при записи». Это неточно. Таблица α^k считается в float64, но хранится в float32 (`_decay_table`), и вся
арифметика event-driven в float32 идёт в float32. Точнее: time-step умножает на округлённое α на каждом шаге,
event-driven — на α^k, округлённое один раз.

В M2.5 скомпилированные ядра с тем же порядком операций разошлись на том же шаге 3,349, что согласуется с этим
объяснением. Формулировки в LN-012 и M2_RESULTS §2 исправлены с пометкой.

