# Допущения

Три разных вида утверждений держим раздельно:

- **A. OBSERVED DATA** — что содержит релиз (со ссылкой на источник или на проверку в `results/`).
- **B. PROCESSING CHOICES** — наши технические решения при обработке. Это не биология; каждое можно поменять и перезапустить.
- **C. SIMULATION ASSUMPTIONS** — то, чего коннектом не знает. В Milestone 1 модели нет, поэтому раздел пока
  содержит только список открытых вопросов, а не принятые допущения.

Если утверждение нельзя отнести к A, оно не может звучать как факт.

## A. Observed data (FlyWire FAFB 783, Zenodo 10676866 + flywire_annotations)

| # | наблюдение | источник |
|---|---|---|
| A1 | Один мозг одной взрослой самки (7 дней), весь мозг вместе с optic lobes, без VNC; сенсорные и восходящие аксоны обрезаны на границе мозга | Dorkenwald 2024; Zheng 2018 |
| A2 | Только химические синапсы; электрические (gap junctions) в данных отсутствуют | Dorkenwald 2024 |
| A3 | Синапс = автоматически предсказанная пара pre/post точек (Buhmann 2021), отфильтрованная по cleft score > 50; полиадный синапс записан как несколько синапсов | Dorkenwald 2024; Zenodo |
| A4 | Ложноотрицательные синапсы — основной тип ошибки; точность детекции ниже у части сенсорных нейронов и вне центрального мозга | Dorkenwald 2024; CAVE tutorial |
| A5 | Нейромедиатор предсказан по изображению синапса (6 классов: GABA, ACh, Glu, Oct, Ser, DA); ко-трансмиссия, нейропептиды, гистамин и прочие не представлены | Eckstein 2024 |
| A6 | Таблица связей содержит только пары proofread-нейронов; синапсы на непроверенные фрагменты в граф не входят | Zenodo; проверка F1–F2 в `results/data_validation.md` |
| A7 | Слабые рёбра плохо воспроизводятся между полушариями и особями («больше 50 % графа — snowflake», в основном слабые рёбра) | Schlegel 2024 |
| A8 | Изображения FAFB зеркально инвертированы; метки L/R в аннотациях исправлены, геометрия — нет | Codex FAQ; Dorkenwald 2024 |

Числовые итоги (нейроны, синапсы, пары, строки) — только из `results/data_validation.md`, не из памяти.

## B. Processing choices

| # | решение | альтернатива | где |
|---|---|---|---|
| B1 | **Ребро** = упорядоченная пара нейронов (pre, post) с ≥ 1 синапсом в любом neuropil; вес = сумма `syn_count` по строкам пары | держать ребро на уровне (pre, post, neuropil) | `preprocess` |
| B2 | Вероятности нейромедиатора ребра = среднее по строкам, взвешенное числом синапсов; хранятся как uint8 (`rint(255·p)`, ошибка ≤ 0.002) | float32 на 6 классов (×4 памяти) | `preprocess` |
| B3 | Self-connections (pre == post) **сохраняются** в store как наблюдение и **исключаются** из всех графовых статистик | удалять на этапе preprocess | `Graph.from_connectome` |
| B4 | Для анализа используются аннотации flywire_annotations **v3.1.0**; v2.1.0 — только чтобы сверить с числами статей 2024 года | Codex (смесь источников, требует логин) | `catalog` |
| B5 | Два порога графа: ≥ 1 синапс (всё наблюдаемое) и ≥ 5 (порог Dorkenwald 2024 и Lin 2024, делает сравнение возможным) | развёртка порогов — позже | `analyze` |
| B6 | **Home neuropil** нейрона = neuropil с наибольшим числом его пресинапсов (все партнёры); без пресинапсов — по постсинапсам. Повторяет правило neuropil-блоков Lin 2024 | по постсинапсам; по сумме pre+post | `analysis.regions` |
| B7 | Clustering считается на **простом неориентированном** представлении (как `global_clustering` в graph-tool у Lin 2024) | ориентированные варианты clustering | `stats.clustering` |
| B8 | Длины путей оцениваются BFS из случайных источников (по умолчанию 200) внутри гигантской SCC (ориентированно) и WCC (неориентированно) | точный all-pairs (N² ≈ 1.9·10¹⁰ пар) | `stats.path_lengths` |
| B9 | Degree-preserving null = перестановки Maslov–Sneppen (igraph `rewire`, 10 попыток на ребро, простой граф), сохраняются in- и out-степень каждого узла; по умолчанию 5 выборок (у Lin 2024 было 100, поэтому z-оценки ориентировочные) | switch-and-hold / больше выборок | `graph.degree_preserving_null` |
| B10 | Сообщества: Leiden по modularity (resolution 1) на неориентированном графе с весом «синапсы в обе стороны»; итерации до прироста < 1e-4 или лимита времени | CPM, другие разрешения, ориентированная modularity | `stats.leiden` |
| B11 | Betweenness оценивается алгоритмом Brandes из k случайных источников с масштабом n/k; две независимые выборки, стабильность top-100 публикуется | точный расчёт O(N·E) | `stats.sampled_betweenness` |
| B12 | Rich-club cut-off: минимальная степень k, начиная с которой нормированный коэффициент > 1.01 при всех бо́льших k (N_k ≥ 10) | порог 1.01 взят у Lin 2024; правило выбора k — наше | `analyzer.null_summary` |

## C. Simulation assumptions (принятые в Milestone 2 — см. раздел E)

Коннектом **не определяет** следующее, а любой SNN этого потребует. С Milestone 2 каждый пункт — явное
допущение в разделе E, часть из них проверена на чувствительность:

- знак и сила синапса (вес ≠ число синапсов; глутамат в мухе бывает тормозным; ошибки предсказания нейромедиатора, особенно у редких классов);
- модель нейрона, пороги, постоянные времени, рефрактерность, спайковый vs градуальный нейрон;
- задержки проведения;
- электрические синапсы, нейромодуляция, нейропептиды, внутреннее состояние;
- правила пластичности.

Для справки (не принято): Shiu et al. 2024 (LIF всего мозга FlyWire) подобрали один свободный параметр
(вес синапса), остальные параметры взяты из литературы как общие. Подробнее — `docs/RESEARCH.md`.

## D. Гипотезы (не факты)

- H1–H3 из постановки проекта — это гипотезы, которые эксперимент может опровергнуть.
- «Модуль/сообщество = функциональный блок» — **гипотеза**; сообщества Leiden описывают только связность.
- «Узел с высокой betweenness = узкое место обработки» — **гипотеза**; это структурная метрика кратчайших путей.

## E. Milestone 2 — параметры spiking-модели

**CONNECTOME ≠ COMPLETE BRAIN MODEL.** FlyWire даёт только первую строку таблицы (число синапсов) и
*предсказания* нейромедиатора. Всё остальное — наш выбор. Значения по умолчанию лежат в
`src/biobrain/snn/config.py` (`PROVENANCE`), описание модели — в `docs/SIMULATOR.md`.

| параметр | значение по умолчанию | класс | откуда / почему | проверка чувствительности |
|---|---|---|---|---|
| число синапсов на ребро | из store | **OBSERVED** | FlyWire 783 (Buhmann 2021, cleft > 50) | — |
| предсказанный нейромедиатор | `top_nt` нейрона | **OBSERVED** (предсказание, не истина) | Eckstein 2024: 94 % на нейрон, серотонин 33 % | источник знака: нейрон / ребро |
| тип модели нейрона | current-based LIF, delta-синапсы | **ASSUMED** | простота, точная ленивая утечка, компактное состояние | — (M2: один baseline) |
| единица потенциала | θ = 1, покой 0 | **ASSUMED** | безразмерная модель; милливольты не утверждаются | масштаб входит в gain |
| dt | 1 мс | **ASSUMED** | обычный шаг для LIF; все времена кратны dt | — |
| τm | 20 мс | **ASSUMED** | порядок величины из литературы (Shiu 2024 использовал 20 мс) | 10 / 40 мс |
| v_reset | 0 | **ASSUMED** | = покою | код поддерживает v_reset < θ (тесты) |
| рефрактерный период | 2 мс | **ASSUMED** | порядок величины (Shiu 2024: 2.2 мс) | 5 мс |
| задержка | 1 мс, одна на все рёбра | **ASSUMED** | FlyWire не содержит задержек; Shiu 2024: 1.8 мс | 2 / 5 мс |
| преобразование f(число синапсов) | linear | **ASSUMED** | число синапсов — не биологический вес | sqrt / log1p / clipped(20) |
| нормировка f | среднее f по 15.1 M рёбер | **DERIVED** | одинаковый вес пары в любом подграфе | — |
| gain | по калибровке для каждого подграфа | **TUNED** | воспроизводимая процедура (`SIMULATOR.md` §9), не под задачу | ×0.5 / ×2 |
| знак ACh | +1 | **ASSUMED** | никотиновые рецепторы — быстрое возбуждение | — |
| знак GABA | −1 | **ASSUMED** | ионотропные GABA-рецепторы — торможение | — |
| знак glutamate | −1 | **ASSUMED**, спорно | GluCl в ЦНС мухи тормозной (так у Shiu 2024 и Pospisil 2024), но глутамат бывает возбуждающим | +1 |
| знак dopamine / serotonin / octopamine | 0 (не быстрый синапс) | **ASSUMED** | модуляторы действуют не как быстрые синапсы; в работах противоречиво (Shiu +1, Pospisil −1) | +1 / −1 |
| нет предсказания нейромедиатора | 0 | **ASSUMED** | 614 нейронов (0.4 %) | — |
| источник знака | нейрон (закон Дейла) | **ASSUMED** | совпадает с поребёрным предсказанием лишь на 79.6 % рёбер | edge |
| порог по числу синапсов | ≥ 1 | **ASSUMED** | все наблюдаемые рёбра | ≥ 5 |
| вход: вес внешнего события | 2θ (вызывает спайк) | **ASSUMED** | контролируемая активность, не сенсорные данные | — |
| вход: паттерны и частоты | по экспериментам | **ASSUMED** | синтетические | poisson / burst / pulse / sparse pattern |
| правила режимов DEAD / STABLE / SATURATED | 1.05 / 50 % / 10 % | **ASSUMED** (определение) | записаны до калибровки; сырые метрики сохранены для переклассификации | — |
| допуск эквивалентности | float64 1e−9, float32 1e−4 | **ASSUMED** (определение) | объявлен до прогонов | — |
| тип чисел | float32 (бенчмарки), float64 (проверки) | **ASSUMED** | компромисс память/точность | эквивалентность в обоих на ≤ 1,000 шагов; на 40,000 шагах float32 time-step расходится с float64 (LN-012) |

**Чего в модели нет вообще:** электрических синапсов, нейромодуляции как процесса, синаптической
динамики (депрессия/фасилитация), нижней границы потенциала (реверсивного потенциала), дендритов, шума,
градуальных (не спайковых) нейронов, пластичности. Это не «нули», а отсутствующие механизмы.
